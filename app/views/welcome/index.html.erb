<h1>Listing users</h1>

<table>
  <tr>
    <th>Token</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @users.each do |user| %>
  <tr>
    <td><%= user.token %></td>
    <td><%= link_to 'Send notification',  controller: "welcome", action: "send_notification", token: user.token%></td>
  </tr>
<% end %>
</table>